<!-- #define {users} -->
<div class="list">
	<!-- ## A comment -->
	<!-- #for user, ${users.asList()}, it -->
	<div class="user ${it.odd?'odd':''}">
		<!-- #if !user.active -->
		<span>${m['not.visible']}</span>
		<!-- #elseif user.age > 18 -->
		<span>${m['adult']({user.name})}</span>
		<div class="bio">
			<!-- #direct -->
			${user.bio}
			<!-- #end -->
		</div>
		<!-- #else -->
		<span>${m['child']}</span>
		<!-- #end -->
	</div>
	<!-- #end -->
</div>